{
    "version": 2,
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/node"
        },
        {
            "src": "pages/api/**/*.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [{ "src": "/(.*)", "dest": "/" }]
}
