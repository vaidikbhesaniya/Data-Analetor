{
    "version": 2,
    "builds": [
        {
            "src": "next.config.js",
            "use": "@vercel/static-build"
        },
        {
            "src": "pages/api/**/*.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [{ "src": "/(.*)", "dest": "/" }]
}
